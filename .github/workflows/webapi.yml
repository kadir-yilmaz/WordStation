name: WebAPI (CI/CD)

on:
  workflow_dispatch:
    inputs:
      run_tests:
        description: 'Testler çalıştırılsın mı?'
        required: true
        type: boolean
        default: false

jobs:
  build-and-test:
    name: 🧪 Build & Test API (CI)
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    - name: Restore
      run: dotnet restore
    - name: Build Solution
      run: dotnet build WordStation.sln -c Release --no-restore
    - name: Run API Tests
      if: ${{ inputs.run_tests == true }}
      run: dotnet test WordStation.Tests -c Release --no-build

  deploy:
    name: 🚀 Deploy WebAPI (CD)
    needs: build-and-test # CI adımı başarısız olursa Deploy ASLA çalışmaz
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    - name: Restore
      run: dotnet restore
    - name: Create appsettings.json from Secrets
      shell: pwsh
      env:
        APPSETTINGS_JSON: ${{ secrets.API_APPSETTINGS_JSON }}
      run: |
        $env:APPSETTINGS_JSON | Out-File -FilePath "WordStation.WebAPI/appsettings.json" -Encoding utf8
    - name: Deploy API
      shell: pwsh
      env:
        WEB_DEPLOY_JSON: ${{ secrets.WEB_DEPLOY_JSON }}
      run: |
        $rawJson = $env:WEB_DEPLOY_JSON | Out-String
        $deployConfig = $rawJson | ConvertFrom-Json
        $server = $deployConfig.WebAPI.Server
        if (-not $server.StartsWith("http")) {
            $server = "https://$server/MsDeploy.axd"
        }
        $site = $deployConfig.WebAPI.SiteName
        $user = $deployConfig.WebAPI.UserName
        $pass = $deployConfig.WebAPI.Password

        dotnet publish WordStation.WebAPI -c Release `
          /p:WebPublishMethod=MSDeploy `
          /p:MSDeployPublishMethod=WMSVC `
          /p:MsDeployServiceUrl="$server" `
          /p:DeployIisAppPath="$site" `
          /p:UserName="$user" `
          /p:Password="$pass" `
          /p:AllowUntrustedCertificate=true `
          /p:EnableMsDeployAppOffline=true `
          /p:SkipExtraFilesOnServer=true
